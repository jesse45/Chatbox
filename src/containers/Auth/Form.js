import React from 'react'

function Form(props) {
    let inputElement = null;

    switch (props.elementType) {
        case value:

            break;

        default:
            break;
    }

    return (
        <div>

        </div>
    )
}


export default Form;